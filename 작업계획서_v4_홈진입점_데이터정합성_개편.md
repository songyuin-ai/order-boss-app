# 사장님앱 통계 프로토타입 v4 - 홈 진입점 데이터 정합성 개편 (POS/HPC 지표 분리 + 절대량 기반 FOMO)

> 대상: `stats-prototype-v3` → `stats-prototype-v4`로 신규 생성 (`songyuin-ai/order-boss-app`, 브랜치 `claude/boss-app-v4-data-consistency`)
> 배경 문서: 사장님앱 데이터 소스 전략 회의록 검토 결과 — OLAP/gCRM 재사용 불가, 분석시스템 매출이 POS 실매출과 미세하게 다름, POS·HPC 두 시스템을 하나의 비율로 묶으면 정합성이 깨진다는 리스크가 v3 코드에 실제로 존재함을 확인
> 목적: v3 홈 화면의 "POS 데이터를 먼저 보여주고 → 그 안에서 POS/HPC를 섞은 비율로 상세 고객분석을 유도"하는 방식을 데이터 정합성 문제로 제외하고, ① 홈에서 상세 고객분석 리포트로 가는 진입점은 유지하되 ② 적립·사용 건수를 늘리고 싶게 만드는 FOMO 장치를 HPC 절대량 기준으로 재설계
> 상태: **확인 대기 (구현 전)** — 8장 확인 후 코딩 시작

---

## 1. v3 코드에서 확인된 문제 (근거)

`stats-prototype-v3`를 확인한 결과, 사용자가 지적한 "POS 데이터 → HPC 연계 상세분석 유도" 패턴이 실제로 다음 세 곳에 존재합니다. 셋 다 **분자는 HPC(해피포인트), 분모는 POS 전체**라서 두 시스템이 미세하게라도 어긋나면 비율 자체가 흔들리는 구조입니다.

| 위치 | 코드 | 문제 |
|---|---|---|
| `HomeScreen.tsx` (`data_041`) | `membershipOrderPct = HPC 적립·사용 주문건수 ÷ POS 전체 주문건수` | 홈 화면 최상단 후킹 카드("최근 30일 상세분석 리포트 보기")의 핵심 수치이자, 지역 평균과도 이 비율로 비교 |
| `MembershipCustomerAnalysisScreen.tsx` (`data_044`) | `memberOrderPct = segmentData.all.orderCount / segmentData.posTotalOrders` | `KpiStrip`에 "전체 주문의 {memberOrderPct}%"로 노출 |
| `SegmentStoryCard.tsx` | `revenueContributionPct` = 세그먼트 매출 ÷ **POS 전체 매출** | 세그먼트별 "매출기여도" 타일 |

이번 v4는 **홈 화면 진입점(위 표의 1번)** 을 명확히 재설계 대상으로 삼습니다. 2·3번은 동일한 근본 문제라 7장에서 확장 여부를 확인받습니다.

---

## 2. 핵심 원칙 (v4 신규)

1. **하나의 지표에 서로 다른 시스템(POS/HPC)의 숫자를 분자·분모로 섞지 않는다.** 비교가 필요하면 같은 시스템 내 절대값끼리 비교한다 (예: 이 매장의 HPC 주문건수 vs 주변매장의 HPC 주문건수).
2. **홈 → 상세 고객분석 리포트 진입점은 유지한다.** 다만 진입 카드의 핵심 수치를 "POS 대비 비율"이 아니라 "HPC 절대량"으로 바꾼다.
3. **FOMO(적립·사용 건수를 늘리고 싶게 만드는 장치)는 유지하되, 비교 기준을 전체 매출이 아니라 HPC 데이터 절대량(주문건수) 기준 주변매장 비교로 바꾼다.**

---

## 3. 변경 A — 홈 화면 상세분석 진입 카드 재설계

### Before (v3, 문제 있음)
```
전체 주문 중 해피포인트 적립/사용한 37.8% 주문 건 기반으로,
손님 상세분석 리포트를 보여드려요

적립·사용이 늘어날수록 리포트는 더 정확하고 쓸모 있어져요.

지역 평균보다 12.2%p 낮아요  [주변매장 평균 50%]
```
→ `37.8%`도, `주변매장 평균 50%`도 모두 "HPC 적립·사용 주문 ÷ POS 전체 주문"이라는 혼합 비율 — POS·HPC 두 시스템 중 하나라도 집계 오차가 생기면 이 숫자 전체가 흔들림.

### After (v4)
```
최근 30일 해피포인트 적립·사용 주문 210건 기반으로,
손님 상세분석 리포트를 보여드려요

적립·사용이 늘어날수록 리포트는 더 정확하고 쓸모 있어져요.

주변매장 평균보다 84건 적어요  [주변매장 평균 294건]
```
→ `210건`과 `주변매장 평균 294건` 모두 **HPC 시스템 하나에서만** 나온 절대값(적립·사용 주문 COUNT). POS 전체 주문건수는 이 카드 어디에도 등장하지 않음. 카드를 탭하면 기존과 동일하게 멤버십 고객 분석 화면으로 드릴다운(진입점 유지, 요건 ②).

- 케이스 전환(낮음/높음) 데모 버튼은 그대로 유지하되 대상 값을 `%`가 아니라 `건`으로 교체 (예: low 210건 / high 340건)
- 톤 로직(warning/success)도 그대로 유지: 주변매장 평균보다 적으면 위기감(적립 유도), 많으면 우월감·유지 동기

---

## 4. 변경 B — FOMO 장치를 HPC 절대량 기준으로 재설계

- **비교 지표: 적립·사용 주문건수(HPC, 최근 30일)** — "적립·사용 건수를 늘려야겠다"는 목적과 가장 직접적으로 연결되는 값이라 매출이나 손님수보다 이 지표를 1차로 채택 (고객수 대안은 8장 확인사항 참고)
- **비교 대상: 주변매장의 동일 지표(HPC 적립·사용 주문건수) 평균** — 매출이나 POS 주문건수와는 절대 비교하지 않음
- **표현 방식**: `210건` vs `주변매장 평균 294건`처럼 **둘 다 절대값**으로 병기. "몇 %"라는 표현 자체를 이 카드에서 없애 비율 계산의 여지를 원천 차단
- **딜리버리 카드와의 구분 안내 문구도 갱신**: (v3) "위 상세분석 리포트 비율은 주문건수 기준, 딜리버리 점유율은 매출 기준으로 서로 다른 지표예요" → (v4) "위 리포트는 적립·사용 주문 건수(절대값) 기준, 딜리버리 점유율은 매출 비중(%) 기준으로 서로 다른 지표예요"

---

## 5. 데이터 모델 변경

### `src/data/homeDummy.ts`
```ts
export interface HomeLast30 {
  // (변경) POS 분모 없는 HPC 단일소스 절대값. 홈·멤버십 화면 모두 이 값을 기준으로 삼음
  memberOrderCount: number;
  regionAvgMemberOrderCount: number;
  deliveryRevenuePct: number; // 유지 (매출 기준, 별도 섹션)
}

// (변경) MEMBERSHIP_ORDER_PCT_BY_CASE → MEMBER_ORDER_COUNT_BY_CASE, 단위 %  건
export const MEMBER_ORDER_COUNT_BY_CASE = {
  low: 210,
  high: 340,
};
```
- `last30.membershipOrderPct` / `membershipRegionAvgPct` 필드 제거, 위 필드로 교체
- `posTotalOrders`처럼 POS 값을 분모로 끌어오는 계산 자체를 홈 화면 쪽에서는 완전히 제거

### `src/data/homeIndicators.ts` (`data_041` 재정의)
| 항목 | Before | After |
|---|---|---|
| 지표명 | 최근 30일 상세분석 리포트 커버리지 (포인트 연관 주문 비율) | 최근 30일 해피포인트 적립·사용 주문건수 |
| 정의 | 최근 30일 전체 주문 중 포인트 적립·사용 주문의 **비율** | 최근 30일간 포인트를 적립·사용한 주문의 **건수(HPC 단일 소스)** |
| 원천데이터_및_산식 | 포인트 적립/사용 주문 수 **÷ 전체 주문 수** | HPC 적립/사용 주문 COUNT (POS 매핑·나눗셈 없음) |
| 제공목적 | 상세분석 가능 범위를 비율로 안내 | 상세분석 리포트의 데이터 볼륨을 절대값으로 안내하고, 주변매장 대비 격차로 적립 유도 |
| 비고 | — | ※ 데이터 정합성 문제로 POS 대비 비율 산식 폐기 (v4) |

### `src/screens/HomeScreen.tsx`
- `membershipOrderPct` 관련 파생 계산(`regionGapRaw` 등)을 `%` 차이가 아니라 `memberOrderCount - regionAvgMemberOrderCount` 건수 차이로 변경
- 헤드라인 문구·비교 배지 문구를 3장 "After" 예시대로 교체
- 클릭 시 `onNavigate("membership")` 이동 로직은 그대로 유지 (진입점 보존)

---

## 6. 화면/컴포넌트 변경 목록

| 파일 | 변경 내용 |
|---|---|
| `stats-prototype` → `stats-prototype-v4` | v3 전체를 복사해 신규 디렉터리로 시작 (v1·v2·v3와 동일한 컨벤션) |
| `src/data/homeDummy.ts` | `HomeLast30` 필드 교체, `MEMBER_ORDER_COUNT_BY_CASE`로 이름 변경 |
| `src/data/homeIndicators.ts` | `data_041` 정의 재작성 (5장 표) |
| `src/screens/HomeScreen.tsx` | 후킹 카드 문구/계산 로직 교체, 딜리버리 카드 안내 문구 갱신 |
| `.github/workflows/deploy-stats-prototype.yml` | v4 디렉터리도 빌드 대상에 포함되도록 배포 워크플로 갱신 |
| `README.md` (루트) | v4 저장소/경로 안내 갱신 |

---

## 7. 제안 — 동일 문제가 남아있는 다른 화면 (범위 확장 여부 확인 필요)

1장에서 확인했듯, 멤버십 고객 분석 화면 자체에도 같은 "POS 분모" 패턴이 두 곳 더 있습니다. 이번 요청은 **홈 진입점**에 한정되어 있어 아래 두 항목은 별도 확인 없이 손대지 않았습니다. 동일한 정합성 원칙(2장)을 적용해 함께 정리할지 확인 부탁드립니다.

| 위치 | 현재 | 정합성 원칙 적용 시 대안 |
|---|---|---|
| `KpiStrip.tsx` — "멤버십 손님" 타일의 "전체 주문의 {memberOrderPct}%" | `data_044`, HPC 주문건수 ÷ POS 전체 주문건수 | "전체 주문의 X%" 문구 제거하고 손님 수·주문건수 절대값만 노출, 주변매장 비교도 절대값으로 | 
| `SegmentStoryCard.tsx` — "매출기여도" 타일 | 세그먼트 매출 ÷ POS 전체 매출 | 타일 제거 또는 "세그먼트 매출(절대값)"로 대체 |

---

## 8. 확인 필요 사항 (컨펌 후 코딩 시작)

1. **FOMO 비교 지표 최종 선택**: 4장에서 "적립·사용 주문건수"를 1차안으로 잡았습니다. 요건 원문에 "HPC 주문건수 OR 고객수"로 둘 다 언급되어 있어, 고객수(멤버십 손님 수, 기존 `data_033`) 기준으로 바꾸길 원하시면 알려주세요. (주문건수 vs 고객수 중 택1, 또는 둘 다 병기)
2. **7장 범위 확장 여부**: 멤버십 화면 내부의 `data_044`(전체 주문 대비 %)와 `revenueContributionPct`(매출기여도)도 이번 v4에서 함께 절대값 기준으로 바꿀지, 아니면 홈 화면만 우선 정리하고 후속 과제로 남길지
3. **홈 후킹 카드 톤 기준값**: v3는 지역 평균 대비 낮음/높음의 기준을 %p 차이로 판단했습니다(`regionGapRaw`). 절대값(건수) 기준으로 바뀌면 "몇 건 차이부터 warning/success로 볼지" 별도 임계값이 필요 없이 그냥 부호(+/-)로 판단하면 되는지, 아니면 최소 차이(예: ±10건 이내는 중립) 같은 완충 구간을 둘지
4. **신규 저장소 경로**: v1·v2·v3와 동일하게 `songyuin-ai/order-boss-app` 레포 안에 `stats-prototype-v4` 디렉터리로 추가하는 것으로 진행해도 되는지 (별도 레포 분리 요청이 없었으므로 기본값은 "동일 레포 내 신규 디렉터리"로 가정)
