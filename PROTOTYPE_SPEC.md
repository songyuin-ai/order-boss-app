# 사장님앱 리뉴얼 프로토타입 스펙

## 개요

파리크라상/SPC 사장님앱(HappyOrder) 리뉴얼 PoC 프로토타입.  
단일 HTML 파일(`prototype.html`) SPA. Apple 디자인 가이드 기반.

---

## 구현 화면 목록

| 탭 | 화면 ID | 상태 |
|---|---|---|
| 홈 | `screen-home` | ✅ 완료 |
| 통계 | `screen-stats` | ✅ 완료 |
| 주문 | `screen-orders` | 플레이스홀더 |
| 쿠폰 | `screen-coupon` | ✅ 완료 |
| 상품관리 | `screen-product` | ✅ 완료 |

---

## 화면별 상세 스펙

### 1. 홈 (`screen-home`)

- 기간 토글: 일간 / 주간 / 월간
- KPI 카드 4종: 총매출, 주문건수, 객단가, 취소율
- 미답변 리뷰 뱃지
- 시간대별 주문 바차트
- 8개 바로가기 그리드 (4×2)

---

### 2. 상품관리 (`screen-product`)

#### 목록 UX

- 검색바
- 카테고리 칩 필터 (전체 / 갓구운빵 / 파란라벨 / 빙수아이스크림 / 한입브레드 / 디저트)
- 정렬 드롭다운: **기본순 / 최근 변경 순**
- 상품 행: 썸네일 + 상품명 + 가격 + **상태 뱃지** + chevron

#### 상태 뱃지 색상

| 상태 | 색상 |
|---|---|
| 판매중 | 초록 `#1a7f3c` |
| 품절 | 주황 `#c75000` |
| 비판매 | 회색 `#7a7a7a` |

#### 상태 변경 바텀시트

상품 행 탭 → 바텀시트 오픈

- **세그먼트 컨트롤**: 비판매 처리 / 품절 처리
- **설명 callout** (선택에 따라 동적 교체)
  - 비판매: 앱에서 상품 완전 숨김 — 주문 자체 불가 (시즌 종료·단종)
  - 품절: 상품 노출 유지, 주문 버튼만 비활성 — 재고 회복 시 즉시 재개
- **적용 범위** (품절 선택 시만 표시, 필수): 전체(default) / 픽업만 / 배달만
- CTA 버튼 텍스트·색상 동적 변경

---

### 3. 주문통계 (`screen-stats`)

- 기간 선택: 오늘 / 이번 주 / 이번 달
- 매출 요약 테이블
- 해피포인트 H값 카드 (3단계: H≥150% / 100~150% / <100%)
- RFM 고객 세그먼트 칩 (VIP / 충성 / 신규 / 이탈위험 / 이탈)

---

### 4. 쿠폰 (`screen-coupon`)

#### 쿠폰 목록

- 발행 상태 뱃지: 진행중 / 종료
- `+ 쿠폰 발행` 버튼 → 쿠폰 등록 모달 오픈

#### 쿠폰 등록 모달 (풀스크린)

| 필드 | 타입 | 비고 |
|---|---|---|
| 발행 대상 | 셀렉트박스 | 모든 고객(default) / 첫 주문 / 배달 주문 / 픽업 주문 |
| 할인 방식 | 세그먼트 | 정액 / 정률 |
| 할인 금액 | 숫자 입력 | 정액 선택 시 노출 (원) |
| 할인율 | 숫자 입력 | 정률 선택 시 노출 (%) |
| 최소 주문 금액 | 숫자 입력 | 항상 표시 |
| 최대 할인 금액 | 숫자 입력 | 정률 선택 시만 노출 |
| 유효 기간 | 날짜 범위 | 시작일 ~ 종료일 |
| 발행 개수 | 숫자 입력 | 무제한 체크 시 비활성 |

#### 발행 확인 다이얼로그

CTA(`쿠폰 발행하기`) 클릭 → 모달 위에 confirm 다이얼로그 오버레이

> "쿠폰 발행 시 유효기간 시작일부터 사용자 화면에 노출됩니다. 정말 발행하시겠어요?"

- 취소 → 다이얼로그만 닫힘
- 발행하기 → 다이얼로그 + 모달 닫힘 + 토스트 "쿠폰이 등록됐어요"

---

## 공통 컴포넌트

### 다점포 스위처

상단바 매장명 탭 → 바텀시트에서 매장 선택 → 매장명 교체 + 토스트

### 래프트 드로어 (좌측 햄버거)

- 매장명 + 계정 정보
- 주요 기능: 홈 / 주문관리 / 상품관리 / 주문통계
- 마케팅: 쿠폰 발행 / 리뷰 관리
- 정산·설정: 정산 내역 / 매장 관리 / 알림 설정

---

## 디자인 토큰 (주요)

```css
--primary: #0066cc
--ink: #1d1d1f
--canvas: #ffffff
--parchment: #f5f5f7
--success: #34c759
--danger: #ff3b30
--warning: #ff9500
--r-sm: 8px  --r-md: 11px  --r-lg: 18px  --r-pill: 9999px
```

전체 토큰 → `design-system.html` 참고

---

## 파일 구조

```
order-boss-app/
├── prototype.html      # 인터랙티브 프로토타입 (단일 파일 SPA)
├── design-system.html  # Apple 디자인 토큰 쇼케이스
└── PROTOTYPE_SPEC.md   # 이 문서
```
