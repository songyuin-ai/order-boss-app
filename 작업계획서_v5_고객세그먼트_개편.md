# 사장님앱 통계 프로토타입 v5 — 홈 후킹카드 잠정 보류 + 조회기간 고정 + 고객 세그먼트 전면 개편 + 재구매율 랭킹 신설

> 대상: `stats-prototype-v4` → `stats-prototype-v5`로 신규 생성 (`songyuin-ai/order-boss-app`, 브랜치 `claude/order-boss-app-v5-plan-hv2b8v`)
> 배경 문서: `세그먼트_기준_변경안.md` (recGrd·ordFre 기준값 기반 4분면 세그먼트 재정의안, 사용자 제공)
> 목적: ① 상무 요청으로 UX라이팅이 진행 중인 홈 후킹 카드(`data_012`)는 완성될 때까지 화면에서 잠정 제외(진입 동선은 유지) ② 멤버십 고객 분석 화면의 "지난 주/지난 달" 조회 탭을 없애고 "최근 30일" 고정 기준(1일 1회 배치 갱신)으로 단순화 ③ 기존 단골/신규/일반/휴면 세그먼트를 전량 폐기하고, 사장님이 보자마자 "무엇을 해야 하는지" 알 수 있는 recGrd×ordFre 4분면 세그먼트로 교체 ④ 재방문 주기(고착도) 카드를 재구매율 TOP5 상품 랭킹으로 교체
> 상태: **계획 수립 (구현 전)** — 화면 구조·데이터 모델 확정 후 실제 코드 작업 진행 예정
> 데이터 기준: 이번 계획은 화면·인터랙션·더미데이터 구조까지만 다룸. 지표별 원천데이터/산식/실시간여부(데이터 목록)는 화면 확정 후 별도 요청 시 정리

---

## 0. 변경 범위 요약

| 위치 | v4 | v5 | 비고 |
|---|---|---|---|
| 홈 화면 "지난 달 상세분석 리포트 보기" 카드 | HPC 절대값 기반 후킹 카드 (건수 비교) | **회색 박스 + "UX라이팅 진행 중" 텍스트만 표시** (클릭 시 멤버십 화면 이동은 유지) | 상무 요청으로 포인트 사용금액 안 검토 중, 확정 전까지 노출 보류 |
| 멤버십 고객 분석 상단 "지난 주/지난 달" 탭 | `SegmentedNav` 2탭 (배치 스냅샷 선택) | **탭 제거, "최근 30일" 고정** | 1일 1회 배치가 "어제 기준 최근 30일" 스냅샷을 갱신 |
| 멤버십 고객 분석 "우리 매장 고객 구성" (단골/신규/일반/휴면 파이) | 4개 세그먼트 파이차트 | **폐기** | recGrd×ordFre 세그먼트 비율차트로 대체 |
| 멤버십 고객 분석 "손님 상세 분석" (세그먼트 탭 스토리카드) | 전체/단골/신규/일반/휴면 5탭 | **폐기** | recGrd×ordFre 4세그먼트 상세분석 탭으로 대체 |
| 멤버십 고객 분석 "단골·신규 추이 (최근 3개월)" | 단골/신규 탭 + 월별 그룹 막대차트 | **재설계해서 유지** | "선택된 세그먼트" 기준 월별 추이로 교체, 주변매장 비교는 그대로 유지 |
| 멤버십 고객 분석 "연령/성별 구성" | 파이차트 | **유지** | 구조 변경 없음 |
| 멤버십 고객 분석 "연령대별 인기상품" | 테이블 + 건수 배지 | **유지 (건수 배지만 제거)** | 순위(1~3위)만 노출 |
| 멤버십 고객 분석 "재방문 주기(고착도)" | 큰 수치 카드 | **폐기** | 재구매율 TOP5 상품 랭킹으로 대체, 탭이 없어져 상시 노출 |
| (신규) 재구매율 TOP5 | — | **신규 카드** | 상품별 재구매율 상위 5개 랭킹 |

---

## 1. v5 프로젝트 스캐폴딩 (v4 소스 재사용)

기존 v2→v3→v4와 동일한 패턴으로, `stats-prototype-v4` 전체를 `stats-prototype-v5`로 복사한 뒤 그 위에서 수정합니다.

1. `stats-prototype-v4/` → `stats-prototype-v5/` 전체 복사 (컴포넌트/데이터/스크립트 포함)
2. `stats-prototype-v5/vite.config.ts` — base 경로를 `/order-boss-app/v4/` → `/order-boss-app/v5/`로 변경
3. `stats-prototype-v5/package.json` — `name`을 `stats-prototype-v5`로 변경
4. `stats-prototype-v5/index.html` — 필요 시 타이틀 갱신
5. `stats-prototype-v5/README.md` — v5 변경점 요약으로 갱신 (v4 README 포맷 준수)
6. `.github/workflows/deploy-stats-prototype.yml` 갱신
   - `on.push.paths`에 `stats-prototype-v5/**` 추가
   - `cache-dependency-path`에 `stats-prototype-v5/package-lock.json` 추가
   - "Install dependencies (v5)" / "Build (v5)" 스텝 추가 (`working-directory: stats-prototype-v5`)
   - Combine 단계에 `mkdir -p combined-dist/v5 && cp -r stats-prototype-v5/dist/. combined-dist/v5/` 추가
7. 배포 브랜치: v4와 동일하게 `claude/boss-app-stats-prototype-8syxnr`에 머지되어야 Pages에 반영되는 구조이므로, v5 작업 완료 후 해당 브랜치로의 반영 방법은 별도 확인 — 이번 계획서 자체는 `claude/order-boss-app-v5-plan-hv2b8v`에서 작성

결과적으로 `/order-boss-app/v4/`와 동일한 방식으로 `/order-boss-app/v5/` URL로 접근 가능해집니다.

---

## 2. 홈 화면 — 후킹 카드(`data_012`) 잠정 비활성화

### 배경
상무가 "포인트 사용금액을 넣어 후킹하자"는 요구를 강하게 밀어붙이는 중이라, 카드 문구/수치 기준이 확정되지 않았습니다. 추후 아래 2개 안 중 하나로 확정될 예정이며, 이번 v5에서는 **UX라이팅이 끝날 때까지 화면 자체를 잠그는 것**이 목적입니다.

- **안 A**: 멤버십 적립·사용 **건수(또는 금액)** 노출 안 — v4의 "적립·사용 주문 N건" 절대값 비교 방식을 유지/변형
- **안 B**: 포인트 **사용금액** 노출 안 — 상무가 원하는 방향, 금액 기준 후킹

→ 두 안 모두 이번 v5 범위에는 포함하지 않고, 확정 후 별도 작업계획서로 진행합니다.

### 변경 방식
`HomeScreen.tsx`의 해당 `Card`에서 타이틀·후킹 문구·비교 배지·케이스 전환 데모는 모두 제거하고, **회색 박스 + 중앙 정렬 텍스트만 있는 플레이스홀더**로 교체합니다. 단, 카드를 탭하면 기존과 동일하게 멤버십 고객 분석 화면으로 이동하는 **클릭 인터랙션은 유지**합니다.

```tsx
// Before: <Card title="지난 달 상세분석 리포트 보기" indicator={homeRealtimeIndicators.last30} ...> (전체 후킹 UI)

// After — 클릭 시 기존과 동일하게 멤버십 화면으로 이동 (인터랙션 유지)
<div
  className="hook-card hook-card--placeholder"
  onClick={() => onNavigate("membership")}
  role="button"
  tabIndex={0}
>
  <p className="hook-card__placeholder-text">UX라이팅 진행 중</p>
</div>
```

- 인디케이터 배지(`IdBadge`)는 제거 — 수치·데이터 근거 자체가 없는 상태이므로 지표표 연동 대상에서 제외
- 클릭 시 멤버십 화면으로 이동하던 CTA 인터랙션은 **유지** — 회색 박스를 탭하면 기존과 동일하게 멤버십 고객 분석 화면으로 이동. 문구·수치만 비활성 상태일 뿐 진입 동선은 그대로 둠
- `case-toggle-row`(low/high 데모 전환 버튼)는 비교할 수치가 없어지므로 제거

---

## 3. 멤버십 고객 분석 — 조회기간 체계 변경

### 배경
"지난 주/지난 달" 고정 탭은 v3의 롤링(최근 N일) 방식이 매일 재계산 부담을 유발해 v4에서 배치 스냅샷 방식으로 바꾼 결과물이었습니다. 이번 v5에서는 사장님 입장에서 탭 자체를 없애고 **"최근 30일"** 하나로 고정하기로 했습니다. 다만 v3가 겪었던 "요청마다 재계산" 부담을 재현하지 않기 위해, 아래 방식으로 구현합니다.

- 화면에는 "최근 30일" 고정 문구만 노출, 탭·화살표 없음
- 실제로는 **1일 1회 배치(예: 매일 새벽)** 가 "어제 기준 최근 30일" 스냅샷을 계산해 저장 → 화면은 그 스냅샷을 그대로 보여줌
- 사용자 입장에서는 실시간 롤링처럼 보이지만, 내부적으로는 하루 한 번만 갱신되는 배치 데이터 → 부하 문제 재발 없음
- 이 변경은 세그먼트 비율/상세분석뿐 아니라 KPI 스트립, 연령/성별 구성, 연령대별 인기상품, 재구매율 TOP5 등 **이 화면의 모든 카드에 공통 적용**
- "최근 30일"이 실시간이 아니라 배치 스냅샷 기준임을 KPI 스트립 캡션 등에 짧게 안내 (예: "최근 30일 기준 · 매일 갱신")

### 구현 방식
- `MembershipCustomerAnalysisScreen.tsx` 상단의 `SegmentedNav(PERIOD_TABS)` 및 `tab`/`PeriodTab` 상태 제거
- `byPeriod: Record<"lastWeek" | "lastMonth", ...>` 형태의 더미데이터 구조를 전부 **단일 스냅샷 객체**로 단순화 (`lastWeek`/`lastMonth` 분기 제거)
- `showRevisitCycle = tab !== "lastWeek"` 같은 탭 의존 분기 로직 제거 — 재구매율 TOP5(5장)는 이제 상시 노출

---

## 4. 고객 세그먼트 전면 개편

### 4-1. 폐기 대상
- `customerCompositionIndicators.composition` / `segmentDetailIndicators.segmentStory`가 표현하던 **단골/신규/일반/휴면** 정의 전체
- `SegmentPieChart`가 그리던 4색 파이(단골/신규/일반/휴면), `SegmentStoryCard`의 전체/단골/신규/일반/휴면 5탭 구조
- ("단골·신규 추이" 카드는 폐기가 아니라 4-6절 기준으로 재설계되어 유지됩니다)

### 4-2. 신규 세그먼트 정의 (`세그먼트_기준_변경안.md` 기준)

**기준값 3종**

| 코드 | 명칭 | 정의 | 산출 방법 |
|---|---|---|---|
| `recGrd` | 최근성 점수 기준값 | "최근에 방문했다" 판정 경계 | 브랜드 평균 재구매 주기 × 1.5~2배를 기준 기간으로, 그 안에 거래 있으면 최근성 ≥ recGrd |
| `ordFre` | 거래빈도 기준값 | "자주 온다" 판정 경계 | 브랜드 전체 고객 최근 1년 거래빈도 분포 상위 25~30% 지점 |
| `avgOrd` | 1회 소비액 기준값 | "객단가가 높다" 판정 경계 | 브랜드 전체 고객 1회 소비액 분포 상위 20% 지점 |

**세그먼트 4분면** (recGrd × ordFre로 상호 배타적 파티션, 중복·누락 없음)

| 세그먼트 | 조건 | 사장님 관점 이유 | 노출 지표 | CTA |
|---|---|---|---|---|
| **찐단골** | recGrd 이상 AND ordFre 이상 | 매출 핵심 손님, 관계 유지 필요 | 매출비중%·주문비중%·객단가·인기상품TOP3(비율)·주 방문시간대·인구통계 최다 조합 | "감사 쿠폰 발송" |
| **떠나려는 단골** | recGrd 미만 AND ordFre 이상 | 원래 매출 기둥이었는데 이탈 직전, 최우선 대응 | 매출비중%·주문비중%·객단가·취소율%·마지막방문 경과일·(과거기준) 주 방문시간대·인기상품TOP3 | "컴백 쿠폰 발송" (최우선 강조) |
| **단골 후보 손님** | recGrd 이상 AND ordFre 미만 | 단골 전환 가능성 있는 골든타임 | 매출비중%·주문비중%·객단가·인기상품TOP3(비율)·주 방문시간대·인구통계 최다 조합 | "다음 방문 쿠폰(적립 2배) 발송" |
| **가끔 오시는 손님** | recGrd 미만 AND ordFre 미만 | 액션 대비 효과 낮음, 규모만 파악 | 매출비중%·주문비중% (최소) | 기본 CTA 없음 |

- 매출·주문 비중은 **절대값이 아니라 비율(%)** 로만 노출 (멤버십 샘플링 데이터 한계 반영)
- "떠나려는 단골"의 인기상품/시간대는 최근 데이터가 없어 **과거(활동 시기) 기준**임을 화면에 명시
- "가끔 오시는 손님"은 상세 지표를 최소화
- 세그먼트 분류 자체(recGrd·ordFre 판정용 lookback 계산)도 3장과 동일하게 **1일 1회 배치**로 갱신 (조회기간 고정과 동일한 갱신 주기 사용)

### 4-3. 화면 구성 — 세그먼트 비율 → 세그먼트별 상세분석(탭) → 세그먼트 추이

`MembershipCustomerAnalysisScreen.tsx`의 세그먼트 관련 카드를 아래 3개로 재구성합니다 (순서 고정).

```
[Card] 손님 세그먼트 비율
  - 도넛/파이 차트 (4개 세그먼트, 클릭 가능)
  - 범례에 세그먼트명 + 비율(%) + 인원수
  - "객단가 높은 손님만 보기" 토글 (4-7절)

[Card] 세그먼트별 상세분석
  - SegmentedNav 4탭: 찐단골 / 떠나려는 단골 / 단골 후보 손님 / 가끔 오시는 손님
  - 탭별 상세 지표 + CTA 문구 (4-2절 표 그대로)

[Card] 세그먼트 추이 (최근 3개월)
  - 위에서 선택된 세그먼트 1개의 월별 인원 추이 (우리 매장 vs 주변매장 평균)
  - 별도 탭 없이 위 두 카드와 같은 세그먼트 선택 상태를 공유 (4-4절)
```

### 4-4. 비율차트 ↔ 상세분석 탭 ↔ 추이차트 3단 동기화

- `segment` 상태를 화면(`MembershipCustomerAnalysisScreen`) 레벨에서 관리하고, 비율차트·상세분석 탭·추이차트 **3개 컴포넌트 모두에** 내려줌
- 비율차트에서 특정 세그먼트 조각/범례를 클릭 → `segment` 상태 변경 → 상세분석 탭과 추이차트가 동시에 해당 세그먼트로 전환
- 상세분석 탭을 직접 클릭해도 동일하게 `segment` 상태가 바뀌며, 비율차트 쪽은 선택된 조각에 강조 스타일(굵은 테두리 또는 확대) 적용, 추이차트도 함께 갱신
- 초기 진입 시 기본 선택 세그먼트는 **찐단골**
- 기존 `SegmentPieChart`는 클릭 핸들러·활성 세그먼트 하이라이트가 없으므로, `onSelect(segment)` / `activeSegment` prop을 추가한 새 컴포넌트(`SegmentRatioChart.tsx`)로 교체

### 4-5. 세그먼트별 상세분석 컴포넌트

`SegmentStoryCard.tsx`를 대체하는 `SegmentDetailPanel.tsx` 신설. 세그먼트별로 노출 필드가 다르므로 3갈래 분기:

```tsx
type SegmentKey = "realRegular" | "leavingRegular" | "candidate" | "occasional";

interface SegmentDetailBase {
  customerCount: number;
  salesSharePct: number;   // 매출 비중(%)
  orderSharePct: number;   // 주문건수 비중(%)
}

interface SegmentDetailFull extends SegmentDetailBase {
  // 찐단골 / 단골 후보 손님
  aov: number;
  topProducts: { name: string; pct: number }[]; // 비율 기준 TOP3
  peakHour: string;          // 주 방문 시간대
  demographicTop: string;    // 최다 연령×성별 조합
}

interface SegmentDetailLeaving extends SegmentDetailBase {
  // 떠나려는 단골 (과거 활동 기준 배지 고정 노출)
  aov: number;
  cancelRatePct: number;
  lastVisitDaysAgo: number;
  topProducts: { name: string; pct: number }[]; // 과거 기준
  peakHour: string;                              // 과거 기준
}

interface SegmentDetailOccasional extends SegmentDetailBase {
  // 가끔 오시는 손님: 매출·주문 비중 외 없음
}
```

- CTA 문구는 데이터가 아니라 세그먼트별 고정 카피(4-2절 표)로 컴포넌트 내부에 하드코딩 (추후 UX라이팅 안 나오면 갱신)
- "떠나려는 단골" 탭에는 인기상품/시간대 블록 상단에 "과거 활동 기준" 안내 배지 고정 노출
- "가끔 오시는 손님" 탭은 매출·주문 비중 2개 타일 + CTA 없음 안내문만

### 4-6. 세그먼트 추이 (최근 3개월) — 재설계

기존 "단골·신규 추이" 카드를 폐기하지 않고, "선택된 세그먼트 1개"를 기준으로 한 추이 차트로 재설계합니다. 주변매장 비교는 그대로 유지합니다.

```tsx
interface SegmentTrendPoint {
  month: string;         // "5월" 등, 항상 최근 3개월 고정 (조회기간 탭과 무관)
  storeCount: number;    // 선택된 세그먼트의 우리 매장 인원수
  regionAvgCount: number; // 선택된 세그먼트의 주변매장 평균 인원수
}

type SegmentTrendData = Record<SegmentKey, SegmentTrendPoint[]>;
```

- 4-4절의 공유 `segment` 상태를 그대로 받아 해당 세그먼트의 3개월 추이만 그림 (내부에 별도 탭 없음)
- 차트 형태는 v4와 동일하게 월별 그룹 막대(우리 매장 vs 주변매장 평균) + 월별 차이 배지
- 월별 스냅샷 집계 기준은 각 월 마감 시점(월말) 배치로 계산

### 4-7. 객단가(`avgOrd`) 크로스컷 필터

| 항목 | 내용 |
|---|---|
| 필터명 (화면 노출) | "객단가 높은 손님만 보기" |
| 위치 | 세그먼트 비율 카드 상단 토글 스위치 |
| 동작 | ON 시 4개 세그먼트 전부, 1회 소비액 ≥ `avgOrd`(상위 20% 지점) 고객만으로 인원·비율·상세지표 재계산 |
| 툴팁 | "1회 소비액이 상위 20%에 해당하는 손님입니다" |
| 세그먼트 구조 | 필터 ON/OFF와 무관하게 4개 그룹 구조는 그대로 유지, 값만 재필터링 |
| CTA 카피 | 필터 ON 상태에서는 객단가를 반영한 변형 문구로 대체 가능 (예: "가끔 오시지만 올 때마다 크게 써주시는 손님들이에요. 놓치면 아까워요") |

- 더미데이터 구조상 세그먼트별로 `default` / `avgOrdFiltered` 두 세트를 준비해 토글 시 전환
- "가끔 오시는 손님"에 필터를 켰을 때도 인원이 남아있으면 고가치 저빈도 고객이 존재한다는 뜻이므로, 0명이 되지 않도록 더미데이터에 최소 표본을 남겨둠 (기획 의도 반영)
- 세그먼트 추이 카드(4-6절)는 이번 범위에서는 필터 적용 대상에서 제외 (기본 인원 추이만 표시)

### 4-8. 데이터 파일 변경

- `segmentDetailDummy.ts`: `SegmentKey`를 4종(`realRegular`/`leavingRegular`/`candidate`/`occasional`)으로 전면 교체, `byPeriod`(지난 주/지난 달) 분기 제거 후 단일 스냅샷 × `avgOrdFiltered`(on/off) 조합으로 더미 구성
- `customerCompositionDummy.ts`: `customerComposition`(단골/신규/일반/휴면 칩) 제거. `segmentTrend`는 4-6절 `SegmentTrendData` 구조로 교체(폐기 아님). `demographicDistribution`/`ageGenderRatios`는 유지
- `segmentDetailIndicators.ts` / `customerCompositionIndicators.ts`의 `composition`/`trend`/`segmentStory` 지표 정의는 신규 지표 정의로 교체 (ID·산식은 6장에 따라 추후 확정)

---

## 5. 인구통계 · 인기상품 — 유지 + 인기메뉴 숫자 표시 제거

- "연령/성별 구성" 카드: 구조·데이터 변경 없음
- "연령대별 인기상품" 카드(`AgePopularProductsTable.tsx`): 상품명 옆 건수 배지(`age-product-table__count`, `{item.orders}건`) **제거**, 순위(1~2~3위 컬럼 위치)만 노출

```tsx
// Before
<td key={i}>
  {item.name}
  <span className="age-product-table__count">{item.orders.toLocaleString("ko-KR")}건</span>
</td>

// After
<td key={i}>{item.name}</td>
```

- `customerDetailDummy.ts`의 `{ name, orders }` 타입은 유지해도 무방(내부 정렬용 데이터는 필요) — 화면에만 `orders`를 노출하지 않도록 처리

---

## 6. 하단 — 고착도 카드 제거, 재구매율 TOP5 상품 랭킹 신설

### 6-1. 재구매율 산식 (웹 리서치 기반 제안)

업계 표준 재구매율(Repeat Purchase Rate) 정의는 **"재구매 고객 수 ÷ 전체 구매 고객 수 × 100"** 입니다 (Wall Street Prep, Omniconvert 등 다수 자료 공통, DTC 이커머스 평균 25~30%). 이를 상품 단위로 적용해 아래와 같이 제안합니다.

> **상품별 재구매율 = (기간 내 해당 상품을 2회 이상 구매한 고객 수) ÷ (기간 내 해당 상품을 1회 이상 구매한 고객 수) × 100**

- 분모: 해당 상품을 한 번이라도 산 고객(신규+재구매 전체)
- 분자: 그중 같은 상품을 다시 산 고객
- 상품별로 이 값을 계산해 상위 5개를 랭킹 표시
- 기준 기간은 3장에서 고정한 "최근 30일"을 그대로 사용 (탭이 없어졌으므로 표본 부족 문제로 노출 여부를 고민할 필요가 없어짐)

**참고 (Sources)**
- [Repeat Purchase Rate | Formula + Calculator — Wall Street Prep](https://www.wallstreetprep.com/knowledge/repeat-purchase-rate/)
- [Repeat Purchase Rate (RPR) — Omniconvert](https://www.omniconvert.com/what-is/repeat-purchase-rate-rpr/)
- [What is Repurchase Rate? — Alexander Jarvis](https://www.alexanderjarvis.com/what-is-repurchase-rate-what-is-the-formula-and-why-it-is-important/)

### 6-2. 화면/컴포넌트

- 기존 "재방문 주기(고착도)" 카드(`revisitCycle`, `data_030`) 완전 제거
- 신규 카드 "재구매율 높은 상품 TOP5" 추가, `TopMenuList.tsx` 패턴을 재사용한 `RepurchaseRankList.tsx` 신설, **탭과 무관하게 상시 노출**

```tsx
interface RepurchaseRankEntry {
  rank: number;
  name: string;
  repurchaseRatePct: number; // 소수 1자리
}
```

```tsx
<ol className="menu-list">
  {items.map((item) => (
    <li key={item.rank} className="menu-list__item">
      <span className="menu-list__rank">{item.rank}</span>
      <span className="menu-list__name">{item.name}</span>
      <span className="menu-list__count">{item.repurchaseRatePct.toFixed(1)}%</span>
    </li>
  ))}
</ol>
```

- `customerDetailDummy.ts`에 `revisitCycle` 제거, `repurchaseTop5: RepurchaseRankEntry[]` 추가 (탭 분기 없이 단일 배열)

---

## 7. 데이터 기준 변경 안내

- 이번 v5부터 실시간으로 확보 가능한 데이터 소스는 **온라인(오더 거래원장) 단 하나**뿐이라는 전제가 생겼습니다. 재구매율(상품×고객 단위 구매이력 필요)·세그먼트 분류(recGrd·ordFre 기준 산정에 필요한 거래이력) 모두 이 제약 안에서 설계되어야 합니다.
- "최근 30일" 조회기간도 실시간이 아니라 1일 1회 배치 스냅샷 기준입니다 (3장).
- 이번 계획서는 화면 구조·인터랙션·더미데이터 타입까지만 확정하고, 지표별 `원천데이터`/`산식`/`실시간여부` 컬럼이 포함된 데이터 목록(지표표)은 화면이 확정된 뒤 **별도 요청 시** 정리합니다. (v4까지의 `data_0XX` ID 재부여도 이 단계에서 함께 진행)

---

## 8. 구현 체크리스트

- [ ] `stats-prototype-v5` 스캐폴딩 (1장)
- [ ] 배포 워크플로 v5 스텝 추가
- [ ] 홈 화면 후킹 카드 → 회색 플레이스홀더 (클릭 인터랙션 유지) (2장)
- [ ] 멤버십 고객 분석 상단 "지난 주/지난 달" 탭 제거, "최근 30일" 고정 문구로 교체 (3장)
- [ ] `byPeriod`(lastWeek/lastMonth) 더미데이터 구조를 단일 스냅샷으로 단순화 (전 카드 공통)
- [ ] `SegmentRatioChart` 신규 컴포넌트 (클릭 선택 + 하이라이트)
- [ ] `SegmentDetailPanel` 신규 컴포넌트 (4세그먼트 분기 + CTA)
- [ ] `SegmentTrendChart` 재설계 (선택 세그먼트 1개 + 주변매장 비교, 비율차트/탭과 상태 공유)
- [ ] 객단가 필터 토글 + 필터드 더미데이터 세트
- [ ] `segmentDetailDummy.ts` / `customerCompositionDummy.ts` / 관련 indicators 재작성
- [ ] `AgePopularProductsTable`에서 건수 배지 제거
- [ ] "재방문 주기(고착도)" 카드 제거, `RepurchaseRankList` 신규 컴포넌트 + 더미데이터 (상시 노출)
- [ ] `MembershipCustomerAnalysisScreen.tsx` 레이아웃 재배치 (세그먼트 비율 → 상세분석 → 세그먼트 추이 → 인구통계/인기상품 → 재구매율 TOP5 순)

---

## 9. 결정 이력 (대화로 확정된 사항)

1. 홈 후킹 카드 플레이스홀더 클릭 시 멤버십 화면 이동 **유지**로 확정 (2장)
2. 멤버십 고객 분석 화면의 "지난 주/지난 달" 탭 **완전 제거**, "최근 30일" 고정 + **1일 1회 배치 갱신**으로 확정 (3장)
3. "단골·신규 추이" 카드 **폐기하지 않고 유지**, 선택된 세그먼트 기준 추이로 재설계 + 주변매장 비교 유지로 확정 (4-6절)
4. 재구매율 TOP5는 조회기간 탭이 없어지면서 자연스럽게 **상시 노출**로 확정 (6장)

현재 추가로 열려 있는 확인사항은 없습니다. 이견 있으시면 언제든 말씀해주세요.
