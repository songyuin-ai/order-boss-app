import type { Indicator } from "./types";
import { posIndicators } from "./posIndicators";

// 진행 중인 현재 기간(오늘/이번 주/이번 달)에서만 실시간이고, 화살표로 과거 기간을 조회하면 마감된 배치 데이터가 표시됨
const TAB_REALTIME_NOTE = "진행 중인 현재 기간(오늘/이번 주/이번 달)만 실시간이며, 화살표로 과거 기간을 조회하면 마감된 배치 데이터예요.";

// "날짜별 보기" 드릴다운 화면(일간/주간/월간 탭). 매출/건수/객단가/온오프라인은 POS 지표(data_001/002/005/006)를 그대로 재사용
// (v4) "채널별 매출(오프라인+배달앱별 breakdown)"은 개별 배달채널 매출이 POS가 아닌 오더 거래원장 소스라
// POS 기준 화면에 노출하는 게 데이터 정합성에 맞지 않아 제거 (딜리버리 탭의 채널별 매출로만 확인)
export const homeIndicators: Record<string, Indicator> = {
  revenue: posIndicators.totalRevenue,
  orders: posIndicators.totalOrders,
  aov: posIndicators.aov,
  // (v4) 주간/월간 탭에만 노출되는 일평균 지표 — POS 지표(data_003/004)를 그대로 재사용
  dailyAvgRevenue: posIndicators.dailyAvgRevenue,
  dailyAvgOrders: posIndicators.dailyAvgOrders,
  onlineOffline: posIndicators.onlineOffline,
  // (v4) 주간=누적, 월간=평균으로 산식 자체가 달라서 딜리버리(data_019/020)와 같은 기준으로 분리.
  // 홈 카드(구 data_039)와도 같은 데이터라 통합 — 홈에서는 항상 이번 주만, 날짜별 보기 주간 탭에서는
  // 화살표로 과거 주까지 조회 가능. 홈 화면 UI에만 있는 요일별 객단가 스트립·주간누적총액 배지도 이 지표 정의에 포함
  // (v5) 홈 카드는 POS 전일자 마감 데이터만 표시 — "오늘"은 아직 마감되지 않아 데이터가 없으므로 마감된 날짜까지만
  // 표시하고 강조색 처리도 하지 않음. 날짜별 보기 주간 탭은 기존과 동일하게 진행 중인 오늘까지 포함해서 조회 가능(TAB_REALTIME_NOTE 참고)
  weekdayCumulative: {
    id: "data_007",
    지표명: "요일별 매출 누적 + 주간 총액 (날짜별 보기 주간 / 홈)",
    실시간여부: "Y",
    실시간참고: TAB_REALTIME_NOTE,
    정의:
      "이번 주(월요일부터) 요일별 누적 매출·객단가 + 주간 누적 총액(지역 평균 대비 비교 배지). 홈 카드는 전일자 마감 데이터까지만 " +
      "표시(오늘은 아직 마감 전이라 미표시)하고, 날짜별 보기 주간 탭은 진행 중인 오늘까지 포함해서 표시. 아직 지나지 않은 요일은 " +
      "표시하지 않음. 진행 중인 현재 주(날짜별 보기의 기본 진입 시점)는 지역 평균 대비 배지만 표시하고 전주 대비 증감은 표시하지 않음, " +
      "날짜별 보기에서 화살표로 마감된 과거 주를 조회하면 전주 대비 배지도 함께 표시",
    원천데이터: "",
    산식: "",
    제공목적: "요일 패턴과 주간 매출 추이, 지역 평균 대비 위치를 함께 파악. 홈에서는 전일 마감 기준으로, 날짜별 보기에서는 과거 주까지 조회",
    차트형태:
      "요일별 막대차트 (월~일 7개). 홈 카드에서는 상단에 주간 누적 총액+지역 평균 대비 배지, 요일별 매출·객단가 스트립도 함께 표시 " +
      "(날짜별 보기 주간 탭에서는 막대차트만 노출, 오늘은 강조색)",
  },
  weekdayAverage: {
    id: "data_008",
    지표명: "요일별 매출 평균 (날짜별 보기, 월간)",
    실시간여부: "Y",
    실시간참고: TAB_REALTIME_NOTE,
    정의: "월간 탭 기준, 해당 월 중 마감된 요일 발생분의 평균",
    원천데이터: "",
    산식: "",
    제공목적: "요일 패턴 파악",
    차트형태: "요일별 막대차트 (월~일 7개)",
  },
  // (v4) 일간=실측치, 주간·월간=일평균으로 산식이 달라서 분리 (요일별 매출과 같은 이유). 주간·월간은 계산식이 같아 하나로 공유
  hourlyOrders: {
    id: "data_009",
    지표명: "시간대별 주문건수 (날짜별 보기, 일간)",
    실시간여부: "Y",
    실시간참고: TAB_REALTIME_NOTE,
    정의: "일간 탭 기준, 해당 일 시간대별 누적 주문건수(실측치)",
    원천데이터: "",
    산식: "",
    제공목적: "시간대별 피크 파악",
    차트형태: "세로 막대차트 (2시간 단위 7구간)",
  },
  hourlyOrdersAvg: {
    id: "data_010",
    지표명: "시간대별 주문건수 일평균 (날짜별 보기, 주간·월간)",
    실시간여부: "Y",
    실시간참고: TAB_REALTIME_NOTE,
    정의: "주간·월간 탭 기준, 경과일 기준 시간대별 일평균 주문건수",
    원천데이터: "",
    산식: "",
    제공목적: "시간대별 피크 파악 (평균 패턴)",
    차트형태: "세로 막대차트 (2시간 단위 7구간)",
  },
  topProducts: {
    id: "data_011",
    지표명: "인기상품 TOP3 (날짜별 보기)",
    실시간여부: "Y",
    실시간참고: TAB_REALTIME_NOTE,
    정의: "선택 탭·기간 기준 주문건수 상위 3개 상품, 각 상품의 전체 매출 대비 비중 표시",
    원천데이터: "",
    산식: "",
    제공목적: "판매량 기준 인기 상품 파악",
    차트형태: "순위 리스트 (이름 + 주문건수 + 매출 비중 배지)",
  },
};

// (v5) 상세분석 리포트 후킹 카드는 포인트 사용금액 기반 카피가 UX라이팅 진행 중이라 회색 플레이스홀더로 대체됨 (2장 참고).
// 지표 ID data_012는 폐기가 아니라 "pending" 상태로 예약 유지 — 사용자 제공 지표관리 시트(ver_5) 기준.
// UX라이팅 확정 전까지는 실제 데이터 근거가 없어 화면에 IdBadge는 노출하지 않음 (지표표 연동 대상에서만 제외)
export const homeHookCardIndicatorPending: Indicator = {
  id: "data_012",
  지표명: "HPC 데이터 기반 후킹영역 (pending)",
  실시간여부: "N",
  정의:
    "상무 요청으로 포인트 사용금액 기반 후킹 카피가 확정되지 않아 화면에는 회색 플레이스홀더만 노출 중. " +
    "카피·수치 기준 확정 후 정식 정의 예정(2장 참고). ID는 예약 상태로 유지하며 다른 지표에 재사용하지 않음",
  원천데이터: "HPC",
  산식: "",
  제공목적: "(확정 전)",
  차트형태: "(확정 전) — 확정 전까지는 회색 박스 + \"UX라이팅 진행 중\" 텍스트만 노출, 클릭 시 멤버십 화면 이동만 유지",
};
