# 사장님앱 통계 프로토타입 v4 - 지표 표 구글시트 연동 개발계획서

> 대상: `stats-prototype-v4` (`songyuin-ai/order-boss-app`)
> 배경: v1/v2에는 구글시트 연동 코드(`lib/googleSheet.ts`, `lib/loadLiveData.ts`)가 이미 있었으나, v3부터는 "화면 구조가 먼저 정리된 뒤 별도 요청 시 연동"이라는 이유로 더미데이터만 사용해왔음. 이제 그 연동을 진행
> 범위 확정 (요청 반영):
> 1. **지표 표(우측 데이터 목록에 나오는 항목들)만** 구글시트로 관리. 차트에 실제로 찍히는 더미 수치(매출/주문건수 등)는 이번 범위에서 건드리지 않음 — 계속 TS 더미데이터 그대로
> 2. 지표 표에 **원천데이터, 비고** 컬럼이 필요하고, 이 두 컬럼은 **시트에 빈 칸으로만 두면** 됨(사용자가 직접 채울 예정)
> 상태: **확인 대기 (구현 전)**

---

## 1. 재사용 가능한 기존 코드 (v2)

`stats-prototype-v2`에 이미 구글시트 연동이 구현되어 있었고, 그중 "지표 정의(Indicators 탭)" 부분이 이번 요청과 정확히 일치합니다.

| 파일 | 역할 | 이번에 재사용 가능한 정도 |
|---|---|---|
| `lib/googleSheet.ts` | 구글시트를 공개 공유(gviz JSON) 방식으로 fetch, 값 파싱 헬퍼(`str`/`num`/`bool` 등) | **그대로 재사용 가능**, 서버/인증 불필요 |
| `lib/loadLiveData.ts` | 여러 탭(Indicators, POS_KPI, Delivery_* 등)을 fetch해서 화면별 더미데이터까지 덮어씀 | **"Indicators" 탭 부분만** 필요, 나머지(POS_KPI 등 차트 수치 탭)는 이번 범위 밖이라 가져오지 않음 |
| `context/DataContext.tsx` | `useState` + `useEffect`로 fetch 후 `mergeIndicators()`로 기존 더미 지표 객체를 시트 값으로 통째 교체, 실패 시 조용히 폴백 | **병합 패턴(`mergeIndicators`)만 재사용**, v4 지표 개수(44개)·필드(`실시간여부`/`실시간참고` 등)에 맞게 갱신 |

핵심 동작 방식(v2 기준, 그대로 가져올 부분): 시트의 "Indicators" 탭에 해당 `id`를 가진 행이 있으면, **그 지표 객체를 통째로 시트 값으로 교체**합니다(필드 단위 부분 병합이 아님). 즉 어떤 셀을 비워두면 그 필드는 빈 문자열로 표시됩니다 — 요청하신 "원천데이터·비고는 비워두면 그 칸만 비어 보이고, 나머지 채워둔 컬럼은 그대로 보인다"는 동작이 별도 구현 없이 이 구조로 자연스럽게 됩니다.

---

## 2. 구글시트 스키마 (신규 "Indicators" 탭 1개만)

v2는 여러 탭(POS_KPI, Delivery_Hourly 등)까지 가져왔지만, 이번엔 **딱 1개 탭**만 있으면 됩니다.

| 컬럼명 | 현재 v4 코드상 필드 | 시트 초기 상태 |
|---|---|---|
| `id` | `id` | 채움 (data_001 ~ data_044, 44행) |
| `지표명` | `지표명` | 채움 (기존 코드 값 그대로 이관) |
| `실시간여부` | `실시간여부` (Y/N) | 채움 |
| `실시간참고` | `실시간참고` (탭 기반 화면 예외 각주) | 채움 (해당되는 행만) |
| `정의` | `정의` | 채움 |
| `원천데이터_및_산식` | `원천데이터_및_산식` | **비움** (요청사항 — 사용자가 직접 채움) |
| `제공목적` | `제공목적` | 채움 |
| `차트형태` | `차트형태` | 채움 |
| `참고` | `참고` | **비움** (요청사항 — 사용자가 직접 채움) |

- 44개 행은 현재 8개 `*Indicators.ts` 파일에 흩어진 지표를 그대로 옮겨 시트에 1회 seed
- 이후로는 **이 시트가 원천(source of truth)**, 코드의 TS 객체는 "시트 접근이 안 될 때를 위한 폴백"으로만 남음

---

## 3. 코드 변경 계획

### 3-1. 신규 파일
| 파일 | 내용 |
|---|---|
| `src/config.ts` | `export const SHEET_ID = "..."` — v2와 동일한 패턴(빈 문자열이면 폴백 더미데이터 사용, 데모가 깨지지 않음) |
| `src/lib/googleSheet.ts` | v2 파일 그대로 복사 (수정 불필요) |
| `src/lib/loadIndicators.ts` | v2 `loadLiveData.ts`를 슬림화 — "Indicators" 탭 1개만 fetch해서 `Record<string, Indicator>` 반환 (POS_KPI 등 차트 탭 관련 로직은 제외) |

### 3-2. 수정 파일
| 파일 | 변경 내용 |
|---|---|
| `src/context/DataContext.tsx` | 정적 `const data = {...}` → `useState` + `useEffect`로 변경. 마운트 시 `loadIndicators()` 호출 → 8개 지표 맵(`posIndicators`, `homeIndicators`, `homeRealtimeIndicators`, `deliveryIndicators`, `customerCompositionIndicators`, `customerDetailIndicators`, `segmentDetailIndicators`, `membershipIndicators`, `kpiIndicators`) 각각에 `mergeIndicators(default, byId)` 적용. fetch 실패 시 조용히 폴백(v2와 동일) |
| `src/data/*Dummy.ts`, `src/data/*Indicators.ts` | **변경 없음** — 지표 정의 TS 객체는 시트 연동 실패 시의 폴백 기본값으로 그대로 유지 |

### 3-3. 주의할 v4 특이사항
- `homeIndicators.ts`의 `revenue`/`orders`/`aov`/`onlineOffline`은 `posIndicators`의 객체를 **참조**(`revenue: posIndicators.totalRevenue`)라서, `posIndicators`를 먼저 병합한 뒤 그 병합된 객체를 `homeIndicators`에도 그대로 연결해야 함(이중 병합/불일치 방지)
- `실시간여부`가 타입상 필수 필드라, 시트에 그 값이 비어 있으면(사용자가 실수로 지우는 경우) 빈 문자열이 들어가 배지가 깨짐 → `loadIndicators.ts`에서 `실시간여부`는 `"Y"`가 아니면 무조건 `"N"`으로 방어적으로 처리

---

## 4. 확인 필요 사항 (컨펌 후 코딩 시작)

1. **시트 공유 방식**: v2와 동일하게 "링크가 있는 모든 사용자 - 뷰어" 공개 공유로 가도 되는지 확인 필요합니다. 이 방식이라야 별도 서버·인증 없이 정적 사이트(GH Pages)에서 그냥 fetch가 가능합니다. 사내 데이터라 공개 공유가 부담되면, 비공개 시트 + 인증 연동이 필요한데 이 경우 서버가 필요해져서 이번 범위(정적 배포)를 벗어납니다.
2. **시트 신규 생성 여부**: v1/v2용으로 이미 만들어둔 시트가 있다면(`config.ts`에 있던 시트 ID) 그걸 재사용/탭만 추가할지, 완전히 새 시트를 만들지
3. **폴백 정책**: 원천데이터/비고를 아직 안 채운 행은 화면에 빈 칸으로 보이는데, 이게 의도하신 그대로인지(맞다고 이해했지만 확인차). 혹시 "채우기 전까지는 기존 코드 값을 유지"를 원하시면 병합 로직을 필드 단위로 바꿔야 해서 방식이 달라집니다
4. **갱신 주기**: v2처럼 앱 로드 시 1회만 fetch(이후 시트를 고쳐도 새로고침해야 반영)로 충분한지, 아니면 일정 주기로 재요청이 필요한지
