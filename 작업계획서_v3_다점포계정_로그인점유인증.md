# 사장님앱 통계 프로토타입 v3 - 다점포 계정 스왑 상세화 (로그인/점유인증 · 계정 추가 · 실시간 매출)

> 대상: `stats-prototype-v3` (`songyuin-ai/order-boss-app`)
> 선행 문서: `작업계획서_v3_IA개편_다점포계정.md` 7장 — 당시엔 "인증 화면 자체는 만들지 않음, 이미 인증된 상태부터 시작"으로 범위를 좁혀뒀음
> 이번 문서의 위치: 그 결정을 뒤집는 것이 아니라 **다음 단계로 구체화** — 마이 아이콘 바텀시트 + 계정 추가 플로우에 실제 로그인/점유인증 컨셉과 매장별 실시간 매출을 반영
> 상태: 설계 초안 (구현 착수 전, 확인 필요 사항 있음 — 9장 참고)

---

## 1. 요건 요약

| # | 요건 | 핵심 포인트 |
|---|---|---|
| 1 | 다점포 로그인/스왑 | 매장당 계정 1개(어드민 발급, 자체가입 아님) · 로그인 시 ID/PW + 점유인증 · 스왑은 로그아웃 없이 계정 목록에서 탭 한 번 · 계정 추가는 로그인 상태에서 ID/PW+점유인증으로 소유자 인증 후 목록에 반영 |
| 2 | 실시간 매출 노출 | 계정 목록 바텀시트에 매장별 당일 매출(실시간) + 상단 합계(SUM) |

---

## 2. 현재 프로토타입 상태 (갭 분석)

`src/components/AccountSwitchSheet.tsx` + `src/App.tsx`에 이미 뼈대가 있음:

- ✅ 마이(👤) 아이콘 → 바텀시트 오픈, 매장 리스트, 탭 한 번 스왑 — 이미 구현됨
- ✅ 스왑 시 재인증 없음 (요건 1-2와 일치)
- ❌ **로그인 화면 자체가 없음** — 앱이 항상 `STORE_ACCOUNTS[0]`으로 이미 로그인된 상태로 시작 (선행 문서 7장 "인증 화면 없이 시작" 결정 그대로)
- ❌ **계정 추가 진입점/플로우 없음** — `STORE_ACCOUNTS`가 3개 매장으로 하드코딩된 고정 배열
- ❌ **점유인증 개념이 화면에 전혀 없음** — 최초 로그인이든 계정 추가든 인증 단계 UI 없음
- ❌ **바텀시트에 매출 정보 없음** — 매장명/주소만 노출, 실시간 매출·SUM 없음
- ⚠️ `DataContext.tsx`는 완전 전역 더미데이터(매장별 분기 없음) — 매장을 스왑해도 헤더의 매장명만 바뀌고 차트 데이터는 그대로임. **이번 작업 범위에서는 그대로 둠** (아래 3장 범위 참고)

---

## 3. 범위 정의

### 포함 (In scope)
- 로그인 화면 (ID/PW 입력 → 점유인증 단계) — 앱 최초 진입 시 게이팅
- 계정 스왑 바텀시트 개선: 매장별 당일 실시간 매출 + 상단 SUM, "계정 추가" 진입점
- 계정 추가 플로우: ID/PW + 점유인증 → 성공 시 보유 계정 목록에 추가되고 바텀시트에 즉시 반영
- 계정 정책을 반영한 카피/문구 (매장당 1개, 어드민 발급 등 안내 텍스트)

### 제외 (Out of scope, 별도 과제로 이월)
- 실제 인증 서버/백엔드 연동 — 전부 mock (더미 계정 pool + 클라이언트 상태만으로 시연)
- 매장별로 실제 대시보드 데이터(차트)가 달라지는 것 — `DataContext`는 여전히 전역 더미데이터. 스왑해도 헤더 매장명만 바뀌고 하단 통계 화면 내용은 동일 (2장 참고, 8번 확인사항으로도 등록)
- 로그아웃, 계정(매장) 삭제/해지, 비밀번호 재설정 — 요건에 없어 1차 범위 제외
- 점유인증의 실제 수단(사업자등록번호 진위확인 API, 문자인증 등) — 프로토타입에서는 하나의 mock 방식으로만 대표 구현 (9장 확인 필요)

---

## 4. 계정 정책 반영 (요건 1-1)

- 매장 1개당 계정 1개, **가입이 아니라 어드민에서 발급 → 사장님에게 전달**하는 형태
  → 로그인 화면에 "회원가입" 진입점을 두지 않음. 대신 "계정을 아직 못 받으셨나요?" 같은 안내 카피만 배치(문의/안내 링크는 플레이스홀더)
- 로그인 = ID/PW 입력 + 점유인증 진행 (2단계)
- 프로토타입에서는 백엔드가 없으므로 **더미 계정 pool**(예: `ADMIN_ISSUED_ACCOUNTS`)을 코드에 미리 정의해두고, 로그인/계정추가 시 해당 pool과 대조하는 방식으로 "발급된 계정만 로그인 가능"이라는 정책을 흉내냄

---

## 5. 화면/컴포넌트 설계

### 5-1. `LoginScreen` (신규)
- `App.tsx`에서 세션이 없을 때(`session === null`) `MobileFrame` 내부 콘텐츠 영역에 렌더 (app-bar 없이 풀스크린)
- Step 1: ID/PW 입력 폼 (기존 `PeriodFilterModal` 류의 폼 스타일 재사용)
- Step 2: 점유인증 — 프로토타입 대표 mock 방식 1가지로 구현 (예: 발급 시 전달받은 인증코드 6자리 입력). 정확한 수단은 9장 확인 필요, 우선 mock으로 진행
- 성공 시: 해당 ID에 매핑된 매장 계정 1개로 `session` 생성 → 홈 진입
- 실패 케이스(잘못된 ID/PW, 잘못된 인증코드) 에러 문구도 프로토타입 수준으로 표현

### 5-2. `OwnershipVerificationStep` (공용 컴포넌트)
- 로그인(5-1)과 계정 추가(5-4)에서 동일하게 재사용되는 점유인증 단계 UI
- ID/PW 검증 이후 진입, 성공 콜백만 다르게 연결 (로그인 완료 vs 계정 목록에 추가)

### 5-3. `AccountSwitchSheet` 개선 (요건 1-2 + 요건 2)
- 상단에 **오늘 매출 합계(SUM, 실시간)** 헤더 추가 — 보유 매장 전체의 realtime revenue 합산
- 각 `store-row`에 매장명/주소는 유지하고, **당일 실시간 매출**을 추가 노출 (예: `formatCompactWon` 사용, 우측 정렬)
- 리스트 최하단에 **"+ 계정 추가"** 행 고정 배치 → 탭 시 `AddAccountModal` 오픈
- 기존 `sheet__note` 문구("탭 한 번으로 전환 · 재인증 없음")는 유지하되, 계정 추가는 별도 인증이 필요하다는 점과 헷갈리지 않도록 문구 조정 필요

### 5-4. `AddAccountModal` (신규)
- "로그인된 상태에서" 진입 (세션 있음 전제) — 바텀시트의 "+ 계정 추가"에서 오픈
- 내부 단계는 로그인과 동일하게 ID/PW → 점유인증(5-2 컴포넌트 재사용)
- 성공 시: 이미 보유한 매장이면 중복 안내, 아니면 `session.accounts`에 새 매장 추가 후 모달 닫힘 → 바텀시트 목록/SUM 즉시 갱신

---

## 6. 데이터 모델 변경안

`AccountSwitchSheet.tsx`의 `StoreAccount`를 확장하고, 로그인/계정 pool 개념을 분리:

```ts
// 어드민이 발급한 계정 카탈로그 (mock) — 로그인 시 대조용, ID/PW 포함
interface IssuedAccount {
  loginId: string;
  password: string;      // 프로토타입 mock 값
  verificationCode: string; // 점유인증 mock 값
  store: StoreAccount;
}

// 화면에 노출되는 매장 정보
interface StoreAccount {
  id: string;
  name: string;
  addr: string;
  todayRevenue: number;      // 당일 누적 매출 (실시간 갱신)
  updatedAtLabel: string;    // "방금 갱신" 등
}

// 로그인 세션 (보유 계정 목록)
interface Session {
  accounts: StoreAccount[];
  activeStoreId: string;
}
```

- `todayRevenue`는 `homeDummy.ts`의 `HomeRealtimeMetric` 패턴을 참고해 매장별로 별도 시뮬레이션 (일정 주기 `setInterval`로 소폭 랜덤 증분 — 기존 `homeRealtime.revenue`가 "실시간"을 표현하는 방식과 톤 맞춤)
- 이번 범위에서는 `todayRevenue`만 매장별로 분기하고, 나머지 대시보드 더미데이터(`DataContext`)는 전역 그대로 둠 (3장 Out of scope)

---

## 7. 상태관리 변경 요약 (`App.tsx`)

- 신규 top-level state: `session: Session | null`
- `session === null` → `LoginScreen`만 렌더 (드로어/바텀시트/app-bar 없음)
- `session` 존재 → 기존 `AppShell` 렌더, `storeId` 대신 `session.activeStoreId` 사용
- `AccountSwitchSheet`에 `accounts={session.accounts}` prop 추가 (기존 `STORE_ACCOUNTS` 하드코딩 참조 제거)
- 스왑(`onSwitch`): `session.activeStoreId`만 갱신 (기존과 동일하게 즉시 전환, 재인증 없음)
- 계정 추가 완료 콜백: `session.accounts`에 새 `StoreAccount` append

---

## 8. 인터랙션 플로우 시나리오

```
[최초 접속]
앱 실행 → session 없음 → LoginScreen
  → ID/PW 입력 → (일치) → 점유인증 코드 입력 → (일치)
  → session 생성 (보유 매장 1개) → 홈 화면 진입

[계정 스왑] (요건 1-2)
마이 아이콘 탭 → 바텀시트 오픈
  → 상단 SUM(보유 매장 당일 매출 합계, 실시간) 노출
  → 매장 리스트 각 행에 당일 실시간 매출 노출
  → 다른 매장 탭 → 즉시 전환 (재인증 없음) → 헤더 매장명 변경, 시트 닫힘

[계정 추가] (요건 1-3)
바텀시트 하단 "+ 계정 추가" 탭 → AddAccountModal 오픈
  → ID/PW 입력 → 점유인증 코드 입력 → (일치)
  → 이미 보유한 매장이면 안내 후 종료 / 신규면 목록에 추가
  → 모달 닫힘 → 바텀시트 목록·SUM 즉시 갱신 (새 매장 포함)
```

---

## 9. 남은 확인 필요 사항

1. **점유인증의 구체적 표현 방식** — 프로토타입에서 "인증코드 입력" 1가지로 대표 구현해도 되는지, 아니면 실제 서비스에서 검토 중인 방식(사업자등록번호 뒷자리, 문자인증 등)을 더 구체적으로 흉내내야 하는지
2. **계정 추가 시 중복 매장 처리** — 이미 보유한 매장 계정을 다시 추가하려 할 때 UX (안내만 하고 종료 / 무시하고 갱신 등)
3. **실시간 매출 갱신 주기 및 표기 단위** — 몇 초 간격으로 증분할지, 정확한 원 단위 vs "OO만원" 축약 표기 중 무엇을 기본으로 할지
4. **로그아웃/계정 제거 기능 포함 여부** — 이번 요건에 명시되지 않아 1차 범위에서 제외 제안, 필요 시 후속 과제로
5. **매장별 실제 데이터 분기 여부** — 이번 범위는 헤더 매장명 + 바텀시트 매출만 매장별로 분기하고, 하단 대시보드 전체(차트 등)는 전역 더미데이터 유지 제안. 매장별로 대시보드 전체를 다르게 보여줘야 한다면 별도 과제로 분리 필요 (작업량 큼 — `DataContext` 구조 자체를 매장 단위로 재설계해야 함)
6. **로그인 실패/인증 실패 시 카피** — 잘못된 ID/PW, 잘못된 인증코드 등 에러 문구 톤

---

## 10. 작업 단계 (Phase)

| Phase | 내용 |
|---|---|
| 1 | 데이터/타입 설계 — `StoreAccount` 확장, `IssuedAccount` mock pool, 매장별 실시간 매출 시뮬레이션 훅 |
| 2 | `LoginScreen` + `OwnershipVerificationStep` 구현, `App.tsx`에 `session` 게이팅 추가 |
| 3 | `AccountSwitchSheet` 개선 — SUM 헤더, 매장별 실시간 매출 행, "+ 계정 추가" 행 |
| 4 | `AddAccountModal` 구현 (점유인증 컴포넌트 재사용) + 세션 계정 목록 반영 로직 |
| 5 | QA — 스왑/추가/중복/실패 케이스 확인, 라이트/다크 스타일 점검, 기존 화면(홈/드로어 등) 회귀 확인 |
