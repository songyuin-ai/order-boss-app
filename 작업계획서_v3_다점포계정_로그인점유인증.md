# 사장님앱 통계 프로토타입 v3 - 다점포 계정 스왑 상세화 (로그인/휴대폰 점유인증 · 계정 추가 · 실시간 매출)

> 대상: `stats-prototype-v3` (`songyuin-ai/order-boss-app`)
> 선행 문서: `작업계획서_v3_IA개편_다점포계정.md` 7장 — 당시엔 "인증 화면 자체는 만들지 않음, 이미 인증된 상태부터 시작"으로 범위를 좁혀뒀음
> 이번 문서의 위치: 그 결정을 다음 단계로 구체화. **1차 구현(커밋 `9d59106`)에 대한 피드백을 반영한 리비전** — 점유인증을 "인증코드 6자리"에서 "휴대폰 OTP + 번호 기반 계정 그룹핑"으로 교체하고, 로그인/계정추가 완료 인터랙션과 약관 동의 UI를 추가
> 상태: **설계 리비전, 구현 착수 전 — 컨펌 대기**

---

## 1. 요건 요약 (이번 리비전 반영)

| # | 요건 | 핵심 포인트 |
|---|---|---|
| 1 | 휴대폰 점유인증 기반 로그인/스왑/계정추가 | ID/PW 입력 → **어드민에 기등록된 점주 휴대폰으로 OTP 발송** → OTP 입력 성공 시 완료. **로그인 시에는 같은 번호로 묶인 계정이 전부 자동으로 계정 목록에 노출**됨. 계정 추가는 로그인 상태에서 별도 매장 ID/PW+OTP로 진행 |
| 2 | 실시간 매출 노출 | 계정 목록 바텀시트에 매장별 당일 매출(실시간, 10분 갱신, 원 단위) + 상단 합계(SUM) — 1차 구현 완료 |
| 3 | 로그인/계정추가 완료 인터랙션 | 로그인 성공 시 토스트("{지점명}으로 로그인되었어요"), 지표 데이터 목록은 기본 숨김 + 별도 버튼으로만 오픈(1회 오픈 후 유지). 계정 추가 성공 시 더 눈에 띄는 팝업("{지점명} 계정 추가에 성공하였습니다") |
| 4 | 휴대폰 점유인증 화면 내 약관/개인정보 안내 | 서비스 이용약관 동의(필수) · 만 14세 이상 동의(필수) · [안내] 개인정보 수집 및 이용사항(동의 아님, 안내만) — 목록 + 상세 노출 |

---

## 2. 1차 구현 대비 이번 라운드에서 바뀌는 것

1차 구현(커밋 `9d59106`)에서 만든 `LoginScreen` / `OwnershipVerificationStep` / `AddAccountModal` / `data/accounts.ts`는 다음과 같이 바뀜:

| 항목 | 1차 구현 | 이번 리비전 |
|---|---|---|
| 점유인증 방식 | 매장 계정별 고정 인증코드 6자리 | **휴대폰 OTP 6자리** — 어드민에 등록된 점주 번호로 발송(mock), 번호는 매장이 아니라 **점주 단위**로 관리 |
| 계정-매장 매핑 | ID/PW 1개 = 매장 1개, 각자 독립적인 인증코드 | ID/PW 1개 = 매장 1개는 동일하지만, 여러 매장이 **같은 점주 휴대폰번호**를 공유할 수 있음 |
| 로그인 성공 시 세션 | 로그인한 매장 1개만 `session.accounts`에 들어감 | **로그인에 사용한 번호로 묶인 매장 전체**가 한 번에 `session.accounts`로 들어감 (요건 1) |
| 계정 추가 성공 시 세션 | 대상 매장 1개만 append | 동일하게 **대상 매장 1개만 append** (그룹 전체를 다시 끌어오지 않음 — 3장 하단 확인사항 참고) |
| 로그인 완료 피드백 | 없음 (바로 홈 진입) | 토스트 팝업 추가 |
| 지표 데이터 목록(`IndicatorPanel`) 노출 시점 | 로그인 직후 항상 `MobileFrame` 옆에 노출 | **기본 숨김**, "데이터 목록 열기" 버튼 클릭 시에만 노출(이후 계속 유지) |
| 계정 추가 완료 피드백 | 모달이 바로 닫힘 | 모달이 성공 화면으로 전환 → "확인" 눌러야 닫힘 (더 눈에 띄는 형태) |
| 점유인증 화면 구성 | 인증코드 입력 필드만 | **약관 동의 체크리스트(목록+상세) + OTP 입력** |

---

## 3. 요건 1 상세 설계 — 휴대폰 점유인증 + 번호 기반 계정 그룹핑

### 3-1. 데이터 모델 변경

```ts
export interface StoreAccount {
  id: string;
  name: string;
  addr: string;
}

export interface IssuedAccount {
  loginId: string;
  password: string;
  ownerPhone: string;   // 어드민에 등록된 점주 휴대폰번호 (예: "01012345678")
  store: StoreAccount;
}

export const ISSUED_ACCOUNTS: IssuedAccount[] = [
  { loginId: "gangnam2024", password: "pass1234", ownerPhone: "01012345678",
    store: { id: "gangnam", name: "파리바게뜨 과천점", addr: "경기 과천시" } },
  { loginId: "hongdae2024", password: "pass1234", ownerPhone: "01012345678", // gangnam과 같은 번호(같은 점주)
    store: { id: "hongdae", name: "파리바게뜨 홍대점", addr: "서울 마포구" } },
  { loginId: "bundang2024", password: "pass1234", ownerPhone: "01098765432", // 다른 번호(다른 점주 시나리오용)
    store: { id: "bundang", name: "파리바게뜨 분당점", addr: "경기 성남시" } },
];

// 휴대폰번호 단위로 발송되는 OTP mock (한 번호에 여러 매장이 묶여도 OTP는 번호 기준 1개)
export const OTP_CODE_BY_PHONE: Record<string, string> = {
  "01012345678": "123456",
  "01098765432": "654321",
};

export function findAccountsByPhone(phone: string): StoreAccount[] {
  return ISSUED_ACCOUNTS.filter((a) => a.ownerPhone === phone).map((a) => a.store);
}
```

- 데모 시나리오: `gangnam2024`로 로그인 → 010-1234-5678로 OTP 발송 → 인증 성공 시 **과천점 + 홍대점이 한 번에** 세션에 들어옴 (같은 번호로 묶인 매장이 자동 노출되는 요건 그대로 재현)
- `bundang2024`는 다른 번호(010-9876-5432)를 써서, "로그인 상태에서 계정 추가"로 별도 매장을 붙이는 흐름을 시연

### 3-2. 로그인 플로우 (비로그인 상태)

```
LoginScreen
  Step 1) ID/PW 입력 → "다음"
    → ISSUED_ACCOUNTS에서 매칭되는 계정 탐색, 실패 시 에러 문구
  Step 2) 휴대폰 점유인증 화면 (OwnershipVerificationStep 재사용)
    → 매칭된 계정의 ownerPhone을 마스킹해서 안내 (예: "010-****-5678로 인증번호를 보내드렸어요")
    → 약관 동의 체크리스트 (4장 참고) — 필수 2개 체크 전엔 인증 버튼 비활성
    → OTP 6자리 입력 → "인증하기"
    → OTP_CODE_BY_PHONE[ownerPhone]과 일치하면 성공, 불일치 시 에러
  성공 시:
    → session.accounts = findAccountsByPhone(ownerPhone)  // 번호로 묶인 매장 전체
    → session.activeStoreId = 로그인에 사용한 매장의 id (예: gangnam)
    → 홈 진입 + 토스트 "{활성 매장명}으로 로그인되었어요."
```

### 3-3. 계정 추가 플로우 (로그인 상태)

```
AccountSwitchSheet "+ 계정 추가" → AddAccountModal
  Step 1) 추가할 매장 ID/PW 입력 → "다음"
  Step 2) 휴대폰 점유인증 (동일 컴포넌트 재사용, 대상 매장의 ownerPhone 기준)
    → 약관 동의 + OTP 입력 → 성공
  성공 시:
    → session.accounts에 대상 매장 1개만 append (그룹 전체를 다시 끌어오지 않음 — 아래 확인사항 참고)
    → AddAccountModal이 "성공 화면"으로 전환 (4장 참고) → "확인" 탭 시 닫힘
    → 바텀시트 목록/SUM 즉시 갱신
```

> **확인 필요**: 계정 추가 시에도 로그인처럼 "그 매장 번호로 묶인 다른 매장까지 한 번에 딸려오게" 할지, 아니면 지금 설계처럼 "추가한 매장 1개만" 들어오게 할지 — 요건 원문(1-2)에는 로그인 때만 그룹 노출이 명시되어 있어 **일단 계정 추가는 대상 매장 1개만 추가**하는 것으로 설계했습니다. 다르게 원하시면 알려주세요.

---

## 4. 요건 3·4 상세 설계 — 완료 인터랙션 + 약관 동의 UI

### 4-1. 로그인 완료 — 토스트 + 데이터 목록 기본 숨김

- **토스트**: 로그인 성공 직후 `MobileFrame` 화면 상단에 "{지점명}으로 로그인되었어요." 토스트가 잠깐 노출되고 자동으로 사라짐(약 2.5초, 비침습적)
- **데이터 목록(`IndicatorPanel`) 기본 숨김**: 로그인 직후에는 `MobileFrame`만 화면 중앙에 노출, `IndicatorPanel`은 렌더하지 않음
  - 프로토타입 영역(모바일 프레임 컬럼) 우상단에 **"데이터 목록 열기"** 버튼 배치
  - 버튼 클릭 시에만 `IndicatorPanel`이 옆에 열리고, **한 번 열리면 이후 계속 열린 상태 유지**(다시 닫는 토글 아님, 버튼도 사라짐)

### 4-2. 계정 추가 완료 — 더 눈에 띄는 팝업

- `AddAccountModal` 내부에서 OTP 인증 성공 시, 모달을 바로 닫지 않고 **같은 모달을 "성공" 화면으로 전환**
  - 체크 아이콘 + "{지점명} 계정 추가에 성공하였습니다." + "확인" 버튼
  - "확인"을 눌러야 모달이 닫힘 (로그인 토스트보다 더 강하게 인지되도록 자동 사라짐 없이 명시적 확인 요구)

### 4-3. 휴대폰 점유인증 화면 — 약관 동의 체크리스트

`OwnershipVerificationStep`에 OTP 입력 위쪽으로 아래 목록 추가 (목록 + 상세 모두 노출):

| # | 항목 | 동의 여부 | 상세 |
|---|---|---|---|
| 1 | 서비스 이용약관 동의 | 필수 체크박스 | 펼치기 시 mock 약관 텍스트 노출 |
| 2 | 만 14세 이상 사용자 동의 | 필수 체크박스 | 펼치기 시 mock 안내 텍스트 노출 |
| 3 | [안내] 개인정보 수집 및 이용사항 | **체크박스 없음 (동의 대상 아님, 안내 전용)** | 펼치기 시 mock 수집항목/이용목적 텍스트 노출 |

- 각 항목 우측에 "펼치기" 화살표 → 탭하면 아코디언으로 해당 항목 상세 텍스트가 펼쳐짐 (실제 약관 문구는 자리표시자 수준의 mock)
- 1·2번 항목이 모두 체크되어야 "인증하기" 버튼 활성화 (3번은 체크와 무관)
- 로그인/계정추가 두 플로우 모두 이 화면을 새로 지날 때마다 동일하게 노출(이전에 동의했었는지 기억하지 않음 — 프로토타입 단순화)

---

## 5. 화면/컴포넌트 변경 목록

| 컴포넌트 | 변경 내용 |
|---|---|
| `data/accounts.ts` | `IssuedAccount.verificationCode` 제거 → `ownerPhone` 추가, `OTP_CODE_BY_PHONE` 추가, `findAccountsByPhone` 추가 |
| `components/OwnershipVerificationStep.tsx` | 인증코드 단일 입력 → 마스킹 전화번호 안내 + 약관 동의 체크리스트(아코디언) + OTP 입력으로 확장 |
| `screens/LoginScreen.tsx` | 성공 콜백이 매장 1개가 아니라 `StoreAccount[]`(그룹) + `activeStoreId`를 함께 반환하도록 변경, 성공 시 토스트 트리거 |
| `components/AddAccountModal.tsx` | 성공 시 즉시 닫힘 → "성공 화면" 단계 추가 |
| `components/Toast.tsx` (신규) | 로그인 성공 토스트 공용 컴포넌트 |
| `App.tsx` | `dataListOpened` 상태 추가(기본 `false`), "데이터 목록 열기" 버튼, `IndicatorPanel` 조건부 렌더링, 로그인 성공 시 토스트 트리거 로직 |

---

## 6. 확인 필요 사항 (컨펌 후 코딩 시작)

1. **계정 추가 시 그룹 전체 vs 대상 매장 1개** — 3-3장 하단 참고, 일단 "대상 매장 1개만 추가"로 설계 (요건 원문 기준)
2. **약관 상세 문구** — 실제 약관 원문이 아니라 프로토타입용 mock 텍스트로 채워도 되는지 (예: "본 서비스 이용을 위해 아래 약관에 동의합니다..." 수준의 자리표시자)
3. **"데이터 목록 열기" 버튼 위치** — 모바일 프레임 컬럼 우상단(프레임 바로 위, 오른쪽 정렬)으로 배치 예정. 앱바 안(휴대폰 화면 내부)이 아니라 프레임 바깥(페이지 레벨)이 맞는지
4. **토스트 표시 위치** — 모바일 프레임 화면 상단 내부(앱바 아래)에 슬라이드 다운 형태로 노출 예정, 다른 위치를 원하시면 조정 가능
