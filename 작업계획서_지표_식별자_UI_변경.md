# 지표 상세 노출 방식 변경 - 개발계획서 (v1, 컨펌 대기)

> 대상: `order-boss-app` / `stats-prototype`
> 배경: 기존 "카드 호버 → JSON 오버레이 팝업" 방식이 어색하다는 피드백에 따라, 우측 지표 테이블 + 식별자 클릭 하이라이트 방식으로 교체
> 범위: UI/인터랙션만 변경. 더미 수치 데이터(track1Dummy.ts, track2Dummy.ts)는 건드리지 않음

---

## 1. 요구사항 정리

1. 탭(그룹)별로 사용되는 지표 리스트를 표 형태로 화면 우측에 상시 노출
2. 지표마다 고유 식별자 부여 (예: `data_001`)
3. 차트마다 해당 식별자를 표시. 식별자를 클릭하면 우측 표에서 해당 행만 하이라이트, 나머지는 딤(dim) 처리

## 2. 식별자 부여안

전체 19개 지표에 순번 ID 부여 (기존 지표 정의 그대로, ID만 추가):

| ID | 소속 | 지표명|
|---|---|---|
| data_001 | Track1 | 매출액 |
| data_002 | Track1 | 주문 건수 |
| data_003 | Track1 | 객단가 |
| data_004 | Track1 | 취소율 |
| data_005 | Track1 | 시간대별 분포 |
| data_006 | Track1 | 요일별 누적 |
| data_007 | Track1 | 요일별 평균 |
| data_008 | Track1 | 인기 메뉴 Top N |
| data_009 | Track1 | 배달/픽업 비중 |
| data_010 | Track1 | 채널별 매출 |
| data_011 | Track2-그룹1 | 고객 구성 |
| data_012 | Track2-그룹1 | 단골·신규 추이 |
| data_013 | Track2-그룹1 | 세그먼트 타겟 쿠폰 연결 |
| data_014 | Track2-그룹2 | 선호 상품·카테고리 |
| data_015 | Track2-그룹2 | 방문 시간대 |
| data_016 | Track2-그룹2 | 재방문 주기 |
| data_017 | Track2-그룹3 | 포인트 활용 지표(H값) |
| data_018 | Track2-그룹3 | 세그먼트별 매출 기여도 |
| data_019 | Track2-그룹3 | G-CRM 인근 가맹점 비교 |

Track1은 오늘/이번 주/이번 달 탭이 같은 지표 풀을 공유하므로(요일별 누적·평균만 탭별 예외) ID도 공유. 탭을 바꿔도 우측 표는 "해당 탭에서 실제 쓰이는 지표만" 필터링해서 보여줌.

## 3. 화면 레이아웃 변경

- 기존: 폰 목업 1개만 중앙 표시, 카드 호버 시 반투명 JSON 오버레이
- 변경: **폰 목업(좌) + 지표 표(우)** 2단 레이아웃
  - 우측 표 컬럼: `ID | 지표명 | 정의 | 원천데이터 및 산식 | 제공목적 | 차트형태`
  - 표는 현재 활성 탭/그룹 기준으로 자동 필터링
  - 각 카드 우상단 배지: 기존 `JSON` → 식별자(`data_001`)로 교체
  - 식별자 배지 클릭 → 우측 표에서 해당 행 강조(배경색+굵게), 나머지 행은 흐리게(opacity 낮춤)
  - 같은 배지 다시 클릭하거나 빈 곳 클릭 시 전체 원복

> 참고: "화면 우측"을 폰 목업 380px 내부에 욱여넣기엔 표 내용이 많아 실제 기기에서는 비현실적이라, **데모/프로토타입 페이지 레벨에서 폰 목업 옆에 나란히** 배치하는 안으로 잡았습니다. (실제 프로덕션 앱에서는 별도 바텀시트/모달 방식이 되어야 할 가능성이 높고, 그건 이번 범위 밖입니다.) 이 레이아웃 방향 괜찮으신지 아래 예시 화면 보시고 확인 부탁드립니다.

## 4. 영향 파일 (구현 시)

| 파일 | 변경 내용 |
|---|---|
| `src/data/track1Indicators.ts`, `track2Indicators.ts` | 각 지표 객체에 `id` 필드 추가 (더미 수치 파일은 미변경) |
| `src/components/IndicatorHover.tsx` | 오버레이 팝업 로직 제거, 식별자 배지 + 클릭 이벤트로 교체 |
| `src/screens/Track1Screen.tsx`, `Track2Screen.tsx` | 우측 지표 표 패널 추가, 활성 ID 상태 관리 |
| `src/App.tsx`, `index.css` | 2단 레이아웃, 하이라이트/딤 스타일 |

## 5. 다음 스텝

1. 아래 예시 화면(클릭 가능한 목업)으로 인터랙션/레이아웃 컨펌
2. 컨펌되면 실제 코드 반영 (데이터 파일의 id 필드 추가 포함)
3. 반영 후 GitHub push → 기존 자동배포로 확인
